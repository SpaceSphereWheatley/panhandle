// Frontend version, kept in sync with public/app.html's APP_VERSION and the
// Worker's VERSION on each release (see CHANGELOG.md) — this React app isn't
// live yet, so it currently mirrors whatever public/app.html has.
export const APP_VERSION = "1.9.2";
