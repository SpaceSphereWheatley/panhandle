{
  "name": "panhandle-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/sync-changelog.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "for f in tests/*.test.mjs; do [ \"$f\" = \"tests/worker-unit.test.mjs\" ] && continue; node \"$f\" || exit 1; done"
  },
  "dependencies": {
    "@pushforge/builder": "^2.0.5",
    "focus-trap-react": "^12.0.3",
    "framer-motion": "^11.18.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@vitejs/plugin-react": "^4.3.4",
    "jsdom": "^29.1.1",
    "vite": "^5.4.11",
    "vitest": "^4.1.10"
  }
}
